# Backend API base URL, including /api/v1. Defaults to
# http://localhost:7015/api/v1 in local dev if unset.
VITE_API_BASE_URL=

# Google OAuth Client ID (must match the backend's GOOGLE_CLIENT_ID).
VITE_GOOGLE_CLIENT_ID=
